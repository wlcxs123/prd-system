# 生产环境依赖包
Flask==2.3.3
Flask-CORS==4.0.0
Flask-Session==0.5.0
bcrypt==4.0.1
marshmallow==3.20.1
gunicorn==21.2.0
psutil==5.9.5

# 可选依赖 - 根据需要启用
# Flask-Mail==0.9.1  # 邮件通知
# Flask-Limiter==3.5.0  # 速率限制
# redis==4.6.0  # Redis 会话存储
# psycopg2-binary==2.9.7  # PostgreSQL 支持
# pymysql==1.1.0  # MySQL 支持

# 监控和日志
# sentry-sdk[flask]==1.32.0  # 错误监控
# prometheus-flask-exporter==0.23.0  # Prometheus 监控